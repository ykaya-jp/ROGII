# 残 86 日スケジュール再構成 (2026-05-10 起点)

> 元 plan: `docs/strategy/winning-strategy.dense.md` の Phase 0-6 (88 日) は **2026-05-05 開始想定**だが、Phase 1.6 完了は実際には **2026-05-10 (Day 6)** で **2 日前倒し**。
> deadline: **2026-08-05 23:59 UTC** (= 残 86 日)。賞金 1 位 **$25K** / 2 位 $13K / 3 位 $7K / 4 位 $5K (合計 $50K, Kaggle listing 一致)。
> 競合: 713 teams, 公開 LB top: needless090 **10.081** / romantamrazov **~10.1 (Top 3)** / karnakbaev **10.784**。

## 旧 plan からの差分

1. Phase 1.6 で 5 件 research doc + 公開 Top 8 kernel 回収完了 (進捗良)
2. **新発見 (2026-05-10)**: `karnakbaev/rogii-code-helper-dataset` (1.3GB) に **LB 10.784 完成 model artifacts** (LGB×3, CB, XGB, Ridge meta, OOF parquet, ensemble weights, postproc params, processed train/test) が host 公開 → そのまま blend 投入可能
3. **新発見**: `thermostatic/rogii-tabicl-v2-public-assets` (107MB) = TabICL 2.1.1 + pretrained ckpt → internet disabled Kaggle で TabICL 動作可、needless090 LB 10.081 stack の base model 再現可能
4. **新発見**: `buchananliang/rogii-karnak-top2-public-artefacts` (6.6MB) = Karnak Top2 artifact (Kaggle 内 leaderboard 上位ペア)
5. これらを Phase 3 で blend に流用 → Phase 3 工数を **3 週 → 2 週** に圧縮、Phase 4 (DL) に 1 週前倒し可能

## 新スケジュール (Day 6 起点 = 残 86 日)

| Phase | 期間 | 目標 LB | 主成果物 |
|---|---|---|---|
| **Phase 2 (現)** | Day 6-15 (残 9 日) | LB **11 帯切り** | exp002 LB / exp003 (tysig features) / exp004 (xcorr_tvt + plane_fit residual) |
| **Phase 3** | Day 16-30 (2 週) | LB **10.5 帯** | Beam+PF 自前実装 / karnakbaev artifact blend / TabICL stack |
| **Phase 4** | Day 31-60 (4 週) | LB **9 帯** | PatchTST + Cross-Attn / FORCE2020 pretext pretrain / 15-seed × 5-fold ensemble |
| **Phase 5** | Day 61-80 (3 週) | LB **8 帯 (1 位射程)** | Stacking (A+B+C+karnakbaev OOF) / physics post-proc / pseudo-label |
| **Phase 6** | Day 81-88 (8 日) | 確定 | Sub 1 (CV best) + Sub 2 (LB best) / Kaggle Notebook 9hr 計測 / final monitor |

## Phase 2 内訳 (Day 6-15)

| exp | 内容 | CV 目標 (TVT hidden RMSE) | LB 目標 | 担当 |
|---|---|---|---|---|
| exp002 | LGB residual baseline (tvt_formula 入り) | 13.82 (実測済) | 12.x (LB 待ち) | submitted, RUNNING |
| exp003 | exp002 + tysig features (xcorr_tvt + wls_b_well + multi-scale SC + gr_detrend_resid) | 12.0 ↓ | 11.x | subagent A 作業中 |
| exp004 | exp003 + 6-formation plane-fit residual + Beam Search (5 configs) | 11.0 ↓ | 10.5-11 | Phase 3 1 週目 (要 numba) |

## Phase 3 内訳 (Day 16-30)

### Track 3a — Top3 distill 移植 (1.5 週)

subagent B 出力 (`docs/research/top3-distill.dense.md`) に基づき、6 要素を `src/rogii/` に組み込み:
1. Beam Search × 7 configs (`src/rogii/beam.py`, numba JIT)
2. Numba Particle Filter (`src/rogii/pf.py`)
3. Self-NCC (`src/rogii/ncc.py`)
4. WLS b_well (`src/rogii/tysig.py` extend)
5. CB / TabICL を base model に追加 (`src/rogii/train_v3.py` extend)
6. Stacking pipeline (`src/rogii/stack.py`)

### Track 3b — Host dataset blend (0.5 週, 並列)

karnakbaev_helper artifacts を Kaggle private dataset 化 → 推論 kernel から:
- LGB×3 + CB + XGB を直接 load して predict
- OOF.parquet を自前 OOF と blend して Ridge meta-learner 再学習
- ensemble_weights / postproc_params も読み込んで再現
- 期待: 自前 LB 11.x → karnakbaev 10.784 と blend で **LB 10.3-10.5**

TabICL 流用も並列:
- `thermostatic/rogii-tabicl-v2-public-assets` を private dataset として参照
- TabICL regressor を base model に追加
- needless090 流の stack ブロックを再構築

## Phase 4 内訳 (Day 31-60) — DL 主力

旧 plan と同じ。PatchTST + Cross-Attn (typewell)、masked TVT pretext、15-seed × 5-fold weighted MEDIAN。前倒し 1 週 = pretext pretrain に時間投入。FORCE 2020 / VOLVE / Brendon Hall facies の private dataset 化。

## Phase 5 (Day 61-80) — Stacking & Post-Proc

A + B + C + karnakbaev OOF + TabICL OOF の 5 source meta-learner stacking。Ridge / LGB の比較。weighted MEDIAN ensemble、physics post-proc (Geology snap / dip continuity / PID smooth)、pseudo-label 1 round。

## Phase 6 (Day 81-88) — Final Sub Selection

Sub 1: CV best (overfit minimal, private LB 安全)。Sub 2: ensemble final (上限狙い, public LB best)。Kaggle Notebook で 9hr 内計測 (8hr 切り目標)。Last 24h は monitor のみ。

## リスクと対策 (新規)

1. **karnakbaev artifact ライセンス** — Kaggle public dataset は default Apache 2.0 だが、karnakbaev/needless090/romantamrazov の各 dataset の `LICENSE` を **submit 前に必ず確認**。引用義務がある場合は `submission notes` に記載
   - 兆候: dataset DL 時に LICENSE が見当たらない / non-commercial 制約
   - 対応: 該当 dataset を blend から除外、自前再実装で代替

2. **artifact 再現性** — karnakbaev model を load しても Kaggle 環境で同 LB が出るとは限らない (lib version / dtype / random state)
   - 対策: Phase 3 1 週目で karnakbaev 単体推論 kernel push して LB を実測 (期待 ~10.784)
   - 兆候: 単体 LB が公表値と 0.5 以上ズレる
   - 対応: 自前再学習に切替

3. **TabICL の Kaggle 推論時間** — TabICL 2.1.1 regressor は in-context LM ベースで推論コスト高
   - 対策: Phase 3 で `time.perf_counter()` 計測、9hr 制約に収まる context size を選ぶ

4. **Top3 distill が遅延** — subagent B が時間取りすぎ
   - 対策: Day 9 (= +3 日) までに `docs/research/top3-distill.dense.md` 1.0 が出ない場合は移植 scope を Beam+PF+CB+TabICL の 4 要素に絞る

5. **exp003 が 12.5 を切らない** — tysig features の効きが弱い
   - 対策: Day 12 までに CV 12.5 切らない場合は exp004 (Beam Search) に直行 (exp003 は best feature subset で commit して止める)

## 1 位射程の評価

旧 plan: 単独 LB 6-9 (Phase B Transformer) + ensemble で 5-8。
新方針: **karnakbaev/TabICL artifact blend** で base LB 10.5 を **Phase 3 終了時 (Day 30)** に確保 → Phase 4 DL で 9 帯 → Phase 5 stack で 8 帯 = 旧 plan より **1 ヶ月以上余裕**。

## 次の判定 (= 中央が監視する signal)

- Day 6 (今日) 23:59 までに exp002 LB が出る → leaderboard.dense.md に記録、CV-LB diff を検算 (CV 13.82 vs LB ?)
- Day 8 までに subagent A の exp003 完遂 → kernel push → LB 取得
- Day 8 までに subagent B の Top3 distill 完成 → Phase 3 着手 GO 判定
- いずれかが Day 10 を超えたら scope 縮小 or 救援 (中央が引き取る or codex:rescue)

## 重要ファイル (新規 + 既存)

- `docs/dev/schedule-2026-05-10.dense.md` (本ファイル) — 新スケジュール
- `docs/dev/leaderboard.dense.md` — exp 毎の LB 記録 (作成予定)
- `docs/research/top3-distill.dense.md` — subagent B 出力 (作成中)
- `docs/research/host-datasets.dense.md` — host 公開 dataset 一覧と利用計画 (作成予定)
- `experiments/exp003/notes.md` — subagent A 出力 (作成中)
- `kaggle_kernels/exp00N_lgb/` — Kaggle script kernel (Code Competition 提出経路)
- `submissions/exp00N_*.csv` — local CV pred (Kaggle 上で再生成される version が公式)
